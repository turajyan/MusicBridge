services:
  backend:
    build: ./backend
    container_name: musicbridge-api
    ports:
      - "8080:8080" # Spotify Auth Callback
    env_file:
      - .env
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    container_name: musicbridge-ui
    ports:
      - "80:80"
    depends_on:
      - backend
